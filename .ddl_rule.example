# 迟交衰减规则: <迟交时长上限(小时)> <分数系数>
# 按上限从小到大匹配第一条满足的行; 超过最大上限一律 0 分
# 系数可写小数(0.8)或百分数(80%); 上限可写 24 / 24h / inf

24   0.8    # 24h 内 ×0.8
48   0.6    # 24–48h ×0.6
72   0.4    # 48–72h ×0.4
# >72h 拒收(隐式 0), 也可显式写: inf 0
